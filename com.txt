 -- mail --
set MAIL_SERVER=smtp.googlemail.com
set MAIL_PORT=587
set MAIL_USE_TLS=1
set MAIL_USERNAME=ivantestov4242
set MAIL_PASSWORD=Juventus_96


 -- worker setup --
celery worker -A st_webservice.celery --loglevel=info
celery -A st_webservice worker --pool=eventlet -l info

-- heroku local --
set up config, .env and .Procfile
http://desktop-331phum:5000/
$ heroku local:run flask deploy
$ heroku local

--heroku cloud--
$ heroku maintenance:on
$ git push heroku master
$ heroku run flask deploy
$ heroku restart
$ heroku maintenance:off

$ heroku logs -t
